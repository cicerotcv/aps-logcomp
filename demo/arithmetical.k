void main() {
    integer a, b;
    a = 30;
    b = 10;

    stdout("Arithmetical Operations");

    stdout("a: " $a);
    stdout("b: " $b);

    stdout("");

    stdout("(a + b): " $(a + b));
    stdout("(a - b): " $(a - b));
    stdout("(a / b): " $(a / b));
    stdout("(a * b): " $(a * b));
}